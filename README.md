# personalpromptpack

## Shared storage

The live library is stored in one shared Vercel Blob file:

- `library.json`

To enable saving in production, add this environment variable in Vercel:

- `BLOB_READ_WRITE_TOKEN`
